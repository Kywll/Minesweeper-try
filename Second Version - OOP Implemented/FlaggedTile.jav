public class FlaggedTile extends Tile{

        FlaggedTile(int boardPosition, boolean isClicked) {
            super(boardPosition, isClicked);
            // You can add things like sout(this is a bomb tile)
            // Basically just Polymorphism

        }
}
